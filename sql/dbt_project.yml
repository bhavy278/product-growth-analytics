name: 'growth_analytics_dbt'
version: '1.0.0'
config-version: 2

profile: 'growth_analytics_profile'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  growth_analytics_dbt:
    staging:
      +materialized: view
    marts:
      +materialized: table
