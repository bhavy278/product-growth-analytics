growth_analytics_profile:
  target: dev
  outputs:
    dev:
      type: postgres
      host: localhost
      port: 5436
      user: postgres
      pass: postgres_password
      dbname: growth_analytics
      schema: public
      threads: 4
